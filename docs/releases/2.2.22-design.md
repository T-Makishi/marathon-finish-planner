# 2.2.22 タブ間の保存同期
- 原因：読み込み後の無変更保存が世代を進め、別タブへの通知処理がない。
- 保存済み内容の指紋を管理し、無変更保存を省略。削除時の二重書込みは維持。
- 初期読込みも保存と同じ排他・キューで実行。
- storage・focus・visibilitychangeで最新内容を取得。未保存編集がある時は取り込まず、既存の世代チェックで上書きを拒否。
- 取り込み可否を読み込み完了時に再確認し、同時編集中のデータ消失を防ぐ。
- 検証：無変更保存、別タブ追加・削除、未保存編集保護、競合拒否、既存全テスト、Web/iOSビルド。
